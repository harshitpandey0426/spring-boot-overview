# use wild card * to expose all endpoints
# can also expose individual endpoint with a comma-delimited list
#example of comma-delimited : management.endpoints.web.exposure.include=health,info
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

info.app.name=mySuperCoolApp
info.app.description=A crazy fun app
info.app.version=1.09.0

